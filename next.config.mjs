/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // i18n: {
  //   locales: ['ru', 'ro'],
  //   defaultLocale: 'ru',
  //   localeDetection: false,
  // },
};

export default nextConfig;
