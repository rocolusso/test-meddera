# TODO: расширение блога Meddera

## Текущий статус (10.05.2026)

| Метрика | Значение |
|---------|----------|
| Статей в реестре (`kind: 'article'`) | **306** |
| Хабов | **6** |
| Рубрик топ-6 в продакшен-структуре | **6** / 16 |
| Part 1 + Part 2 по топ-6 | **Готово** (статьи 1–50 для дерматолога/ботокса; 1–49 для остальных четырёх кластеров + 10 inline lip в `registry`) |
| План масштаба (ориентир) | ~1600 статей |

---

## ФАЗА 1: Топ-6 → до ~100 статей на рубрику

**Осталось по кластерам (ориентир):** Part 3 (51–75), Part 4 (76–100) для каждой рубрики + при желании расширение «базового» текста Part 2 до длинного формата.

### 1.1 Дерматолог

- [x] Хаб, Part 1 (25), Part 2 (25) — **50 статей**
- [ ] Part 3: статьи 51–75 — метаданные + контент
- [ ] Part 4: статьи 76–100 — метаданные + контент

Файлы уже в проекте: `dermatologist-cluster-articles-part1/2.ts`, контент в `dermatologist-article-content*.ts` (несколько part). При добавлении партий — новые `partN` и импорты в объединяющий файл контента и в `registry.ts`.

---

### 1.2 Ботокс

- [x] Хаб, Part 1 + Part 2 — **50 статей**
- [ ] Part 3, Part 4 — по 25 статей

Файлы: `botox-cluster-articles-part1/2.ts`, `botox-article-content*.ts`.

---

### 1.3 Увеличение губ

- [x] Хаб, кластер Part 1 + Part 2 — **49** статей в файлах кластера
- [x] Отдельный блок из **10** статей в `registry.ts` + контент в `lip-cluster-article-content.ts` / `lips-article-content*.ts`
- [ ] Дальнейшее наращивание до 100 **в кластере** (Part 3/4) при необходимости

---

### 1.4 Мезотерапия лица

- [x] Хаб, Part 1 (24) + Part 2 (25) — **49 статей**
- [ ] Part 3, Part 4
- [ ] Опционально: расширить ранний базовый текст до длинного формата

---

### 1.5 Биоревитализация

- [x] Хаб, Part 1 + Part 2 — **49 статей**; контент может быть в `biorevitalization-article-content-part2/3.ts`
- [ ] Part 3, Part 4

---

### 1.6 Филлеры / контурная пластика

- [x] Хаб, Part 1 + Part 2 — **49 статей**; часть контента Part 2 в **`fillers-article-content-part3.ts`**
- [ ] Part 3, Part 4 в метаданных и контенте

---

## ФАЗА 2: Остальные 10 рубрик (~100 статей каждая)

План без изменений по смыслу: пилинги, чистка лица, лазер, карбокситерапия, мезотерапия волос, липолитики, альгинатные маски, дермапен, терапия пигментации, анти-акне.

Для каждой рубрики типичный чеклист:

1. `clusterId` в `src/blog-data/types.ts`
2. Хаб + статьи в `registry.ts`
3. `*HubBody.tsx` / `*ArticleBody.tsx` при необходимости отдельного макета **или** переиспользование существующего `bodyKey` и контент-мэпа
4. `renderBlogPostBody` в **`src/blog-data/render-post-body.tsx`**
5. Контент RU/RO, slug без конфликтов
6. Путь услуги из раздела «Услуги» (как в старых версиях этого файла — см. историю или `BLOG_PROGRESS.md`)

Детальные тематики и имена файлов можно переносить из предыдущих версий репозитория при старте конкретной рубрики.

---

## ФАЗА 3: SEO и техника

- [ ] Распределение дат публикации / модификации (регулярный график)
- [ ] JSON-LD: уже частично есть для статей/хабов — довести при росте числа шаблонов
- [ ] `src/app/sitemap.ts` — актуален под динамический список постов из данных
- [ ] Internal linking / «Читайте также» (частично через `getRelatedArticles`)
- [ ] После деплоя: проверка 404 на проде, кеш CDN

Скрипты контроля (папка `scripts/`): `verify-blog-metadata-content.mjs`, `export-blog-article-urls.mjs`, при необходимости `check-all-blog-urls.mjs`, `check-ro-slugs.mjs` и др.

---

## Чеклист новой партии статей

1. Метаданные (`*-cluster-articles-partN.ts`) — уникальные `id`, `slugRu`, `slugRo`
2. Контент — ключ `id` совпадает с метаданными; для fillers/biorevitalization учитывать несколько файлов контента
3. `registry.ts` — spread массива статей
4. `npm run verify:blog-content`
5. `npm run build`

---

## Технические файлы (напоминание)

| Файл | Роль |
|------|------|
| `src/blog-data/registry.ts` | Посты, `getPostBySlug`, `ORIGIN`, **`blogPathRu` / `blogPathRo`** |
| `src/blog-data/render-post-body.tsx` | `renderBlogPostBody` |
| `src/app/(ru)/blog/[slug]/page.tsx`, `(ro)/ro/blog/[slug]/page.tsx` | Страницы; `dynamicParams = false` |
| `next.config.ts` | `webpack`; сборка через **`next build --webpack`** |

---

## Метрики успеха (напоминание)

- Целевой масштаб по бизнес-плану: **1600** статей / **16** рубрик — достигать поэтапно.
- На каждом шаге: **0 ошибок сборки**, `verify:blog-content` зелёный, нет «дырок» в контенте (иначе `notFound()` / 404).

---

**Создано:** 08.04.2026  
**Последнее обновление:** 10.05.2026  
**Версия:** 2.0
